---
license: cc-by-4.0
task_categories:
  - tabular-regression
source_datasets:
  - Dingdong-Inc/FreshRetailNet-50K
tags:
  - sample
  - smoke-test
  - derived
  - stockout-annotated
---

# freshretailnet-h7 — sample dataset

A small, ready-to-upload dataset for smoke-testing the Mitra regressor pipeline. It is
**derived** from FreshRetailNet-50K and is meant to exercise the pipeline end to end, not to
benchmark anything.

## Contents

`freshretailnet-h7.zip` (244 KB) holds the pipeline's CSV contract:

| file | rows | columns |
|---|---|---|
| `train.csv` | 4,180 | 17 features + `target` |
| `val.csv` | 1,600 | 17 features + `target` |
| `test.csv` | 1,600 | 17 features + `target` |

- **Target** — daily `sale_amount` **7 days ahead** (a regression target).
- **Features** — sales history (`lag_1/7/14`, `roll_7_mean`, `roll_28_mean`, `roll_7_std`),
  the stockout signal (`stockout_hours`, `roll_7_stockout`), and same-day covariates
  (`discount`, `holiday_flag`, `activity_flag`, `precpt`, `avg_temperature`, `avg_humidity`,
  `avg_wind_level`, `dow`, `month`).
- Dense target: ~3.4% zeros.

## How it was built

Generated by [`../build_freshretailnet_dataset.py`](../build_freshretailnet_dataset.py) from
the FreshRetailNet-50K `train.parquet`:

```bash
python examples/build_freshretailnet_dataset.py --src <train.parquet> --out ./out \
  --horizon 7 --n-series 220 --max-rows 8000 --seed 0
```

One row per store-product-day; 220 store-product series sampled, then each split capped for
size. The split is a **purged per-series chronological split**: within each series the layout is
`train | embargo | val | embargo | test`, with a `horizon`-length embargo (7 rows) removed at
each boundary. Because a feature row at day `t` carries the target for day `t + 7`, the embargo
guarantees a training row's target cannot fall inside the validation window, and a validation
row's target cannot fall inside test — a strictly forward-looking, leak-free evaluation rather
than the horizon-overlap a plain tail split would still allow.

## Provenance and licence

Derived from **FreshRetailNet-50K** by Dingdong Inc, upstream revision
`08c1fab7f9257bc73679d415d65d644165d351d4`
([source](https://huggingface.co/datasets/Dingdong-Inc/FreshRetailNet-50K)). FreshRetailNet-50K
is **CC BY 4.0**; this derived sample is redistributed under the same licence with attribution.

> Wang, Y., Gu, J., Long, L., Li, X., Shen, L., Fu, Z., Zhou, X., & Xu, J. (2025).
> *FreshRetailNet-50K: A Stockout-Annotated Censored Demand Dataset for Latent Demand
> Recovery and Forecasting in Fresh Retail.* arXiv:2505.16319.

## Note

This is a convenience sample. For real work, build your own dataset from the full corpus with
the script above, or from any `(entity, date, value)` panel following the
[dataset specification](../../TABULAR_REGRESSION_DATASET_SPEC.md).
