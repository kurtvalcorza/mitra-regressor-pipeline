__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
desktop.ini
*.egg-info/
out/

# Local Mitra weights (289MB) — never committed; for the DIMER wizard upload or a local bake.
# Only the snapshot manifest (weights/<MODEL_KEY>/dimer-base-manifest.json), config.json and LICENSE are committable.
weights/**/*.safetensors
weights/**/*.ckpt
weights/**/*.bin
weights/**/*.pt
weights/**/*.pth
weights/**/*.onnx
weights/**/.cache/
