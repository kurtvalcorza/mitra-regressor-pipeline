FROM pytorch/pytorch:2.8.0-cuda12.8-cudnn9-runtime
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends libgomp1 && rm -rf /var/lib/apt/lists/*
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
RUN python -c "import torch; v=torch.__version__; c=torch.version.cuda; assert v.startswith('2.8'); assert c and c.startswith('12.8')"
ENV DIMER_TASK_TYPE=tabular_regression
COPY train.py contract_hardening.py contract_entrypoint.py ./
CMD ["python", "contract_entrypoint.py"]
