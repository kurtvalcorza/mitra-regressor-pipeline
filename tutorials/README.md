# Mitra Regressor tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/mitra-regressor-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/mitra-regressor-pipeline/blob/main/tutorials/mitra_regressor_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-autogluon%2Fmitra--regressor-ffcc4d?style=flat)](https://huggingface.co/autogluon/mitra-regressor)
[![arXiv](https://img.shields.io/badge/arXiv-2508.02927-b31b1b.svg)](https://arxiv.org/abs/2508.02927)

Notebook specification: **DIMER Notebook Specification 2.0** — both notebooks are **standalone** (§4): they are generated by `tools/build_notebook.py` from `tools/notebook_template.py` (E2E) and `tools/notebook_template_artifact_inference.py` (companion) and carry the repository's public tutorial API module (`mitra_pipeline/tutorial_api.py`), model identity, snapshot manifest and runtime pins, so the exported `.ipynb` files keep working without this repository being reachable. Do not edit a notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py` and `python tools/build_notebook.py --template tools/notebook_template_artifact_inference.py`; `--check` is enforced by the validator and CI). A notebook is promoted from Candidate to Release-grade only after clean-runtime execution evidence is recorded for the exact release revision in [`../docs/release-verification.md`](../docs/release-verification.md).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD / external input | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| [`mitra_regressor_colab.ipynb`](mitra_regressor_colab.ipynb) | `E2E` | `GUIDED` | standalone (generated) | verified snapshot → `validate_inputs` → input manifest → overlap/cap reports → in-context evaluation on holdout + independent test (MAE/RMSE/R²) against `training_mean_baseline`, Dummy mean/median, LightGBM and Random Forest → optional GPU fine-tune with holdout-only selection → `evaluation_report` (`sample-sanity`) → optional new-data inference → predictor bundle export with manifest/provenance → fresh reload with prediction equivalence | CPU; GPU only for optional fine-tuning | automatic | `DATA_SOURCE` upload options gated by `USE_BYOD`; `RUN_FINE_TUNING`, `RUN_NEW_DATA_INFERENCE` off by default | verified — clean-runtime `Run all` execution recorded (REL1/REL8) | **Candidate** — static checks pass; the clean-runtime execution run of the standalone carrier is clean-room execution recorded in `../docs/release-verification.md`, which must be reviewed for the exact notebook revision before promotion |
| [`mitra_regressor_predictor_inference_colab.ipynb`](mitra_regressor_predictor_inference_colab.ipynb) | `ARTIFACT-INFERENCE` | `GUIDED` | standalone (generated) | externally supplied predictor ZIP → trusted whole-archive digest → safe extraction → manifest/provenance/base-model/runtime checks before `TabularPredictor.load` → `validate_inputs(target_column=None)` → input manifest → point predictions → `evaluation_report` (`not-measurable`) → export | CPU (CUDA used automatically when available) | **none yet** — default path uploads both the bundle and the rows (§19 SART1/RUN5 gap) | bundle ZIP + one unlabelled CSV, by upload or `ARTIFACT_ZIP_PATH` / `NEW_DATA_PATH` (no bundled sample: the bundle must come from a separate execution) | unverified — no clean-runtime `Run all` execution recorded for this revision (REL1/REL8) | **Candidate** — external-artifact clean-runtime evidence pending; see `../docs/release-verification.md` |

## Conformance notes

_Requirement identifiers in these notes (for example `ST1`, `PAR3`, `DAT24`, `EVAL21`) are the NOTEBOOK_SPEC 1.1 numbering they were written against; NOTEBOOK_SPEC 2.0 renumbered its sections and the pull request that migrated this repository reports conformance against the 2.0 pre-flight checklist (§26) and report format (§30)._

- **Standalone carrier (ST1–ST6):** the primary path performs no clone, repository install or repository import (the previous pair's `git clone` of this repository and its `%pip install -r` are gone: the module is carried instead). Section 2 of each notebook is `mitra_pipeline/tutorial_api.py` verbatim (one cell tagged `metadata.dimer.embedded_module`; the only rewrite makes `DEFAULT_WEIGHTS_DIR` working-directory-relative), Section 3 carries the model identity and the two-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins. `metadata.dimer.generated_from` records the repository, the generating commit, the module SHA-256 and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py`, `tests/test_companion_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when a notebook is not byte-identical to the generator's output for its template.
- **Model acquisition:** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries that are absent, from the Hugging Face Hub at the immutable revision (never `main`); `verify_snapshot` re-hashes them and asserts the manifest digests equal the package's `WEIGHTS_SHA256` / `CONFIG_SHA256`; `MitraRegressionPipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` then calls the existing `stage_verified_hf_snapshot`, which stages the verified bytes as the immutable offline Hugging Face snapshot AutoGluon resolves (`HF_HUB_OFFLINE`). The previous `MODEL_SOURCE = "DIMER ZIP"` option is replaced by the snapshot directory: place the two files there and nothing is downloaded (`validate_dimer_model_package` remains in the API for DIMER packages).
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/<stem>_input_manifest.json` with the schema, the observed structure of every table (the `validate_labeled_frame` report with dropped missing-target rows and exact duplicates — never hidden, DAT23), the verdict and one recorded rejection finding; `evaluation_report` (EVAL21) writes `outputs/<stem>_evaluation_report.json` — `sample-sanity` with `mae` / `rmse` / `r2`, the independent-test block and the training-mean baseline in the E2E notebook (the executable-baseline table is attached), `not-measurable` (with what labelled data would make the task measurable) in the companion.
- **Artifact-inference companion (§18):** the bundle is supplied from outside the execution (upload, or `ARTIFACT_ZIP_PATH` for executors); a trusted whole-archive SHA-256 is required by default (`ALLOW_UNVERIFIED_ARTIFACT` is an explicit override); `safe_extract_archive` rejects absolute paths, traversal, backslash paths, symlinks, suspicious compression ratios, oversized members and oversized expansion; `validate_artifact_directory` verifies the inventory, sizes, digests and provenance, the base-model identity/digests are compared with the carried pinned values and the AutoGluon/Python versions are checked, all before `TabularPredictor.load(...)`; no bundle is created and nothing is fitted in the companion.
- Regression obligations (Notebook Specification §20.2): the target is validated as finite numeric and varying before the model runs, MAE and RMSE are reported in target units with R² alongside, and the notebooks state that predictions are point estimates with no uncertainty interval.
- `USE_BYOD`, `RUN_FINE_TUNING` and `RUN_NEW_DATA_INFERENCE` (E2E) and `ALLOW_UNVERIFIED_ARTIFACT` (companion) default to `False`.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Runtime and model contract

- The runtime pins are the `pyproject.toml` `[project].dependencies` (`==` only), carried inline by both notebooks as `PINS` and mirrored in [`requirements-colab.txt`](requirements-colab.txt): `autogluon.tabular[mitra]==1.5.0`, `lightgbm==4.6.0`, `huggingface-hub==0.36.2` (AutoGluon's own compatible ranges resolve torch, numpy, pandas and scikit-learn transitively, as before).
- Local snapshot layout: `weights/mitra-regressor/` with the committed `dimer-base-manifest.json`, `config.json` and `LICENSE`; `model.safetensors` is git-ignored (see [`../docs/WEIGHTS.md`](../docs/WEIGHTS.md)).
- Sample data: the default sample is scikit-learn's bundled diabetes table; the repository's `examples/sample-data/` archives (FreshRetailNet, Insurance Charges, Ames Housing) are **no longer fetched by the notebook** — a standalone notebook may not reach this repository (ST1) — and are used through `DATA_SOURCE = "Upload pre-split train/val/test"`.

## Model provenance

- Model: `autogluon/mitra-regressor`
- Task: tabular regression
- AutoGluon: `1.5.0`
- Immutable upstream revision: `5f277aa8f69042d39d6ac3612aed18bb9279bd95`
- Weights SHA-256: `d8e75c62af0bec2fd404b0ad20a442d951d43ca6d331315cfcc0509b54f2c642`
- Config SHA-256: `2bc1ed5047f7c25368245e8ad32540a5fa28940b1ec05d3f1f454a09ff5384c1`
- Supported ceiling: 10,000 training rows and 500 features
- Output: one continuous point prediction per row; no calibrated per-prediction interval

The model repository supplies weights/configuration only; Mitra's executable integration comes from the pinned AutoGluon package. The notebooks do not execute Python code from the model repository.

## Data and evaluation

The tutorial sample portfolio is pinned to repository revision `f11bf59d1bb7e75de42145e311de9773fda1607a`, matching the `SAMPLE_REVISION` embedded in the E2E notebook. It contains:

- `freshretailnet-h7.zip` — leakage-aware temporal demand regression derived from FreshRetailNet-50K;
- `insurance-medical-charges.zip` — cross-sectional medical-cost regression; and
- `ames-housing.zip` — cross-sectional residential-price regression.

Each archive's SHA-256 is recorded in the notebook's `SAMPLE_CONFIGS` registry and verified after download; `scripts/test_sample_registry.py` loads every registry variant offline against the committed archives.

All are tutorial/sanity fixtures, not benchmark evidence. Their source/provenance and licence notes are documented in [`../examples/sample-data/DATASET_CARD.md`](../examples/sample-data/DATASET_CARD.md).

For the default FreshRetailNet path, the literal CPU tutorial deterministically uses 512 training rows and 256 rows from each evaluation partition as a bounded smoke subset; users can raise the notebook controls to use more or all rows. BYOD paths are unaffected. A BYOD single-CSV path uses a deterministic random holdout and explicitly assumes approximately IID rows. Time-dependent, grouped, panel, embargoed, patient/device-level, spatial, or otherwise leakage-sensitive workflows should provide pre-split train/validation/test files.

Regression evaluation includes executable constant baselines rather than quoted development-run numbers. MAE and RMSE remain in target units; R² is complementary. Sample values must not be generalized to other datasets.

## Artifact contract

The E2E tutorial exports `mitra-predictor.zip`. The archive contains the selected AutoGluon predictor plus:

- `tutorial_run_metadata.json` — model/revision, runtime, data identity, selected variant, adaptation configuration, metrics, feature order, target, and provenance;
- `artifact_manifest.json` — format/version and complete file inventory with size and SHA-256 for every other artifact file.

Because Mitra inference uses support/training context and AutoGluon preprocessing state, the exported predictor may contain or encode information derived from source data. Apply the source dataset's confidentiality, licensing, disclosure, and retention rules to the artifact.

Path safety and manifest consistency do not make Python serialization safe. Only load predictor ZIPs from trusted producers. The artifact-inference notebook requires a trusted whole-archive SHA-256 by default; bypassing that check is an explicit expert override.

The optional DIMER offline model ZIP has its own normative manifest contract documented in [`DIMER_MODEL_PACKAGE.md`](DIMER_MODEL_PACKAGE.md).

## Release verification

The release gate, the executor procedure for both notebooks and the durable execution record live in [`../docs/release-verification.md`](../docs/release-verification.md). Static checks run in ordinary CI:

- the standalone carrier and parity checks (`tools/validate_release_assets.py`, `tools/build_notebook.py --check`, `tests/test_notebook_parity.py`, `tests/test_companion_parity.py`);
- the repository-specific spec-1.1 checks (`scripts/validate_colab_tutorial.py`), conformance markers and absence of placeholder text;
- CSV/header regression checks against the package and the generated notebooks;
- public API archive/manifest and DIMER-package regression tests.

A separate **Notebook release execution** workflow executes the current E2E default path in a clean hosted runner, produces a predictor artifact, derives new inference rows, and executes the `ARTIFACT-INFERENCE` notebook against that externally produced artifact with its trusted SHA-256. The workflow records actual step outcomes and fails closed unless the producer, digest preparation, and consumer execution all succeed. A release claim must cite the successful workflow/PR head; static CI alone is not execution evidence.

## AI use and provenance

These tutorials have been developed with substantial AI assistance under human direction and review. AI attribution is authorship provenance, not sign-off. Clean execution, static checks, and human review remain the evidence for a release.
