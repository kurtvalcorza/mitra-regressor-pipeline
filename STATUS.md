# Release status

Current status: **Candidate — standalone tutorial source complete, awaiting clean-runtime execution**. The public tutorial API (`mitra_pipeline/tutorial_api.py`, one module in a root-level package), the offline unit suite, `MODEL_CARD.md`, and the standalone tutorial pair — `tutorials/mitra_regressor_colab.ipynb` (`E2E`) and `tutorials/mitra_regressor_predictor_inference_colab.ipynb` (`ARTIFACT-INFERENCE`), both generated by `tools/build_notebook.py` under DIMER Notebook Specification 1.1 §3.6 — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks for both notebooks). The previous repository-installing pair (spec 1.0) was executed by the `notebook-release.yml` workflow in a clean hosted runner; that evidence does not transfer to the standalone carrier, and the workflow needs the standalone executor procedure in `docs/release-verification.md`. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit of the standalone notebooks is recorded there.
